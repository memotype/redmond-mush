# Data

This directory holds redistributable YAML inputs, schemas, and related
generation sources for `redmond-mush`.

Policy:

- YAML is the authoritative source for fixed rules-oriented data that is
  intentionally published here.
- Public example files should stay small, original, and operational.
- Full instance-specific rules inventories do not ship here by default.
