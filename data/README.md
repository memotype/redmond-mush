# Data

This directory holds public-safe YAML inputs, schemas, and related generation
sources for the OSS product layer.

Policy:

- YAML is the authoritative source for fixed rules-oriented data that is
  intentionally published here.
- Public example files should stay small, original, and operational.
- The full official-instance inventory of rules content does not ship here by
  default.
